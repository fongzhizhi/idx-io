{
  "compilerOptions": {
    /* 基础选项 */
    "target": "ES2020",                // 保持良好兼容性，同时支持现代API
    "module": "NodeNext",              // 使用Node.js最新的ES模块标准
    "lib": ["ES2020"],                 // 明确指定依赖的标准库，避免环境隐式注入
    "moduleResolution": "NodeNext",    // 与`module: "NodeNext"`配对
    "outDir": "./dist",                // 编译输出目录，清晰分离源码与构建产物
    "rootDir": "./src",                // 明确指定源码根目录，保持结构清晰

    /* 严格类型检查模式（库项目的核心） */
    "strict": true,                    // 启用所有严格的类型检查选项
    "skipLibCheck": true,              // 跳过对`.d.ts`库文件的类型检查，提升编译速度
    "esModuleInterop": true,           // 确保与CommonJS模块的正确互操作性
    "allowSyntheticDefaultImports": true, // 允许从没有默认导出的模块进行默认导入

    /* 库发布关键选项 */
    "declaration": true,               // 生成`.d.ts`类型声明文件（发布库必须！）
    "declarationMap": true,            // 为类型声明文件生成源码映射，方便使用者导航
    "sourceMap": true,                 // 为JavaScript文件生成源码映射，便于调试

    /* 代码质量与现代化 */
    "forceConsistentCasingInFileNames": true, // 强制文件名大小写一致，避免跨平台问题
    "exactOptionalPropertyTypes": true, // 精确检查可选属性类型，更严格
    "noUncheckedIndexedAccess": true   // 更安全地访问索引属性（如`obj[key]`）
  },
  "include": ["src/**/*"],            // 只编译src目录下的文件
  "exclude": ["node_modules", "dist", "**/*.test.ts", "**/*.spec.ts"] // 排除无需编译的文件
}